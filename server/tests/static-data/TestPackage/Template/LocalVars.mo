within TestPackage.Template;
record LocalVars "Record to Test Local Var references"
    extends Modelica.Icons.Record;

    parameter String local_var="Default Value"
        "A test value for local references";
end PartialTemplate;
